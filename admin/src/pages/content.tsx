import { SectionHeading } from "@/components/ui";

/**
 * Nội dung app — banner, khối nội dung và câu chữ của các trang mini app.
 * (Đang dựng.)
 */
export default function ContentPage() {
  return <SectionHeading title="Nội dung app" subtitle="Đang dựng." />;
}
